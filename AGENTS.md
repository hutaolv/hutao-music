# 项目约定

## 代码注释
- 每次修改代码都必须添加中文注释，说明改动的原因和逻辑。
- 新增函数：在函数上方用 `//` 注释说明用途、参数含义。
- 重要/易错逻辑：在代码行旁加注释说明为什么这样做。
- 注释保持简洁，不写无关内容。

## 响应式（必须遵守）
- 项目需同时适配桌面、平板、手机，三端都要能正常显示。
- 手机端断点统一用 `@media (max-width: 767px)`（避开 iPad 的 768px）；≤640px / ≤480px 用于更窄的细分场景。
- 每次新增页面或组件，必须加上手机端适配样式，且改动只在媒体查询内生效，不得影响桌面/平板默认样式。
- 常见做法：手机上隐藏次要列/按钮、网格改用 auto-fill/minmax、固定宽度面板用 `width: min(固定值, calc(100vw - 边距))` 兜底。
